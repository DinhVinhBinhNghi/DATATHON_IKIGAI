# Config

- `config.example.yaml`: file mẫu có thể commit.
- `config.local.yaml`: file cá nhân, không commit, dùng để lưu path local như `C:/Datathon_Data`.

Các script hiện tại đều nhận `--data-root`, nên bạn không bắt buộc phải tạo `config.local.yaml`.
